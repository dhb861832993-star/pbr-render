# PBRRender dsh bundle layer: mount the host plugin row. The client half is
# auto-discovered by dsh-client-modules via the package's `dsh.client` + 
# `exports["./client"]` declaration (served at /plugins/pbr-render/client.js).
- insert:
    - id: pbr-render
      name: pbr-render
